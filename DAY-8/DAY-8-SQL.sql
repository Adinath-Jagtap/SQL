-- =============================================================================
-- SQL PROBLEMS (3)
-- =============================================================================
/*
1. Find the second highest salary from employees table
   - Return the second maximum salary (handle ties and NULLs appropriately).

2. Find duplicate emails in users table
   - List email addresses that appear more than once (with counts).

3. Delete duplicate rows keeping only one copy
   - Remove duplicate records from a table while retaining a single instance (based on chosen key/columns).
*/


-- Asumptions:
-- employees(emp_id, name, salary)
-- users(id, email)