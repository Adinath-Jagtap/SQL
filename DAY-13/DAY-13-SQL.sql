-- =============================================================================
-- SQL PROBLEMS (3)
-- =============================================================================
/*
1. Find employees hired in the last 6 months
   - Use WHERE with date comparison (CURRENT_DATE / NOW() and INTERVAL).

2. Update salaries by 10% for a specific department
   - Use UPDATE with SET salary = salary * 1.10 and WHERE department = 'X'.

3. Delete records older than a certain date
   - Use DELETE FROM table_name WHERE date_column < 'YYYY-MM-DD'.
*/